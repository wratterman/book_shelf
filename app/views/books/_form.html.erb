<%= form_for @book do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>

  <%= t.label :price %>
  <%= t.text_field :price %>

  <%= t.label :quantity %>
  <%= t.text_field :quantity %>

  <%= t.button :submit %>
<% end %>
